[project]
name = "reader-mcp-server"
version = "0.2.2"
description = "MCP server and CLI tools for Readwise Reader API integration"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "httpx>=0.28.1",
    "mcp[cli]>=1.4.1",
    "pydantic>=2.0",
]
